# Placeholder Syntax

Use `{{NAME}}` for values that should be filled in by code before sending a prompt.

Rules:
- keep placeholders explicit
- replace them only at the final assembly step
- never send unresolved placeholders to the model

